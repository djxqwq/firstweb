{
  "name": "dengjinxin-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:home": "npm run dev --prefix home",
    "dev:about": "npm run dev --prefix about-web",
    "dev:api": "cd backend && .venv\\Scripts\\python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "build:home": "npm run build --prefix home",
    "build:about": "npm run build --prefix about-web"
  }
}
